<Window x:Class="CAPTCHA.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CAPTCHA"
        mc:Ignorable="d"
        Title="Ввод" MaxHeight="250" MaxWidth="250" MinHeight="250" MinWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <WrapPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0">
            <TextBox Height="53" HorizontalAlignment="Left" FontSize="28" Name="inputRANDOM"  Width="200" Margin="10"/>
            <Button Content="Проверить" Height="31" HorizontalAlignment="Left" Name="ButtonRANDOM" Width="200" Click="ButtonRANDOM_Click" Margin="10" />
        </WrapPanel>
        <Label Grid.Row="1" x:Name="labelTIMER" FontSize="16" Margin="10"
               VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</Window>
